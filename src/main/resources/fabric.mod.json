{
	"schemaVersion": 1,
	"id": "midasflip",
	"version": "${version}",
	"name": "MidasFlip",
	"description": "SkyBlock flipping intelligence. Human-executed, never automated: /viewauction fires only on your physical keypress, and the opt-in confirm overlay (off by default, ASSISTED mode only) forwards one physical click as exactly one action. One physical input = at most one game action; STRICT mode sends nothing ever.",
	"authors": ["noobsistaken"],
	"contact": {
		"sources": "https://github.com/noobsistaken/MidasFlip-Client"
	},
	"license": "GPL-3.0-only",
	"environment": "client",
	"entrypoints": {
		"client": ["dev.midasflip.client.MidasflipClient"]
	},
	"mixins": ["midasflip.client.mixins.json"],
	"depends": {
		"fabricloader": ">=0.19.3",
		"minecraft": "~26.1.2",
		"java": ">=25",
		"fabric-api": "*"
	}
}
